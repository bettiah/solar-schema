"""
Fast lookup tables for validation - X12 version {{ version }}.

Auto-generated by SchemaGenerator. Do not edit manually.
"""

# Element type lookup: element_id -> (type, min_len, max_len)
ELEMENT_TYPES: dict[str, tuple[str, int, int]] = {
{% for elem in elements %}
    "{{ elem.id }}": ("{{ elem.data_type.value }}", {{ elem.min_length }}, {{ elem.max_length }}),
{% endfor %}
}


# Segment elements lookup: segment_id -> [element_ids]
SEGMENT_ELEMENTS: dict[str, list[str]] = {
{% for seg in segments %}
    "{{ seg.id }}": [{% for e in seg.elements %}"{{ e.element_id }}", {% endfor %}],
{% endfor %}
}
